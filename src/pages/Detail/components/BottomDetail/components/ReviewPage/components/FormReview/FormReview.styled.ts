import { styled } from '@mui/material/styles';
import { Box, Typography, Button } from '@mui/material';

export const FormReviewWrapper = styled(Box)`
    min-width: 43%;
`;
export const HeaderFormReview = styled(Box)``;
export const TitleFormReview = styled(Typography)``;
export const YourRatingReview = styled(Typography)``;
export const FormRating = styled(Box)``;
export const SubmitReview = styled(Button)``;
